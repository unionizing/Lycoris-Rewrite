# Deepwoken Rewrite
**Module diff vs. previous snapshot: +1/-0/~3 (added/removed/changed)**
```diff
+ (added) ShadowPuppet
+ (changed) RapidSlashes
+ (changed) WeaponUppercutTest
+ (changed) SilentheartMediumRelentless
```

**Timing diff vs. previous snapshot: +2/-0/~6 (animation: +1/-0/~6, part: +1/-0/~0)**
```diff
+ (changed) Animation : 1_GenericDaggerUppercut
+ (changed) Animation : MetalHug
+ (added) Animation : RosenJumpCrit
+ (changed) Animation : 1_GenericHeavyAerial
+ (changed) Animation : FlamethrowerCrit
+ (changed) Animation : ShadowEruptionCast
+ (changed) Animation : ShadowRisingWind
+ (added) Part : ShadowPuppet
```

**New features?**
No new features added.

*Your commit ID should == "be6552" when the update is fully pushed to you.*