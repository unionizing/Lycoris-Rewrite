# Deepwoken Rewrite
**Module diff vs. previous snapshot: no changes detected.**

**Timing diff vs. previous snapshot: +0/-0/~7 (animation: +0/-0/~7)**
```diff
+ (changed) Animation : RandomWindMove
+ (changed) Animation : DaggerAerial
+ (changed) Animation : RunningAttackSword1
+ (changed) Animation : RunningAttackKatana
+ (changed) Animation : BruteHeavyPunch
+ (changed) Animation : BoneBoyRushGo
+ (changed) Animation : HeavyAerial
```

**New features?**
```diff
- (bug fix) Possibly identified why "Auto Flow State" would not work for buyers
+ (added) AP Filter "Disable While Using Sightless Beam"
+ (added) Animation Speed Changer (only affects initial speed / windup)
```

*Your commit ID should == "afb8a7" when the update is fully pushed to you.*